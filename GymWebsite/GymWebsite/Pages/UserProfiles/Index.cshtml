@page
@model GymWebsite.Pages.UserProfiles.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserProfile[0].Height)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserProfile[0].Weight)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserProfile[0].Username)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserProfile[0].User)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.UserProfile) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Height)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Weight)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Username)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.ID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.UserProfileID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.UserProfileID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.UserProfileID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
