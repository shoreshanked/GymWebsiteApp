@page
@model GymWebsite.Pages.Workouts.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Workout[0].WorkoutName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Workout[0].TrainingBlock)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Workout) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.WorkoutName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TrainingBlock.ID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.WorkoutID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.WorkoutID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.WorkoutID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
